# Purpose: Placeholder Secret manifest for the Route 53 credential consumed by cert-manager.
# Important: This file contains placeholders only. Replace them securely at deployment time; never commit real AWS credentials.
apiVersion: v1
kind: Secret
metadata:
  name: route53-credentials
  namespace: cert-manager
type: Opaque
stringData:
  secret-access-key: "<REDACTED>"
